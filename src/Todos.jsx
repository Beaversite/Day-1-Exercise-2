import TodoItem from "./TodoItem";

function Todos({ todos, deleteTodo, checkTodo }) {
  // คำใบ้ก็คือ เราใช้ todos.map(todo => ...อะไรต่ออน้า)
  return (
    <div>
      <p>น้องๆ เติมโค้ดที่ทำการแยก Component ออกมาตรงนี้ได้เลย</p>
    </div>
  );
}

export default Todos;
